# 掼蛋 · Guandan（H5 离线版）

一个完整的掼蛋游戏。**离线优先**（无网络可玩，适合飞机/地铁），基于纯 H5（HTML/CSS/原生 JS，无构建、无框架、无第三方依赖），引擎与界面解耦，便于后续做联机版与多端迁移。

## 运行方式
- **离线**：直接双击 `index.html` 在浏览器打开即可（`file://` 下运行，无需服务器、无需联网）。
- **引擎测试**：`node test/engine.test.js`

## 规则口径
采用**全国竞技掼蛋标准规则**：
- 4 人 2v2，对家为队友；双副牌共 **108 张**（含 2 小王 + 2 大王）。
- 每人 27 张；**级牌**从 2 起打；**红桃级牌为「逢人配」**（万能牌，可代除王外任意牌）。
- 牌型：单 / 对 / 三 / 三带二 / 顺子(5张) / 三连对(木板,6张) / 二连三(钢板,6张) / 炸弹(4+张) / 同花顺 / 天王炸(四王)。
- 炸弹大小：四张炸 < 五张炸 < **同花顺** < 六张炸 < … < 八张炸 < 天王炸；同级比点数。级牌点值抬至 A 之上、王之下。
- 升级 / 进贡还贡 / 抗贡 / 接风 等流程在阶段②实现。
> 如需地方变体（如不同炸弹序、不同升级规则），改 `engine.js` 常量与比较函数即可。

## 目录结构
```
Guandan/
├── index.html              # H5 入口（离线可开）
├── README.md
├── src/
│   ├── engine/engine.js    # ✅ 规则引擎：牌/牌型识别(含逢人配)/比较
│   ├── game/game.js        # ✅ 牌局流程：发牌/进贡还贡抗贡/出牌/接风/计分升级/整场
│   ├── ai/ai.js            # ✅ 完整出牌生成器 + AI 五档（入门→宗师）
│   └── ui/ui.js            # ✅ H5 对局界面控制器（事件驱动，人机对战）
└── test/
    ├── engine.test.js      # ✅ 引擎单测（35 用例全过）
    ├── game.test.js        # ✅ 流程单测（23 用例全过，含整场过A）
    └── ai.test.js          # ✅ AI/生成器/梯度对抗（17 用例全过）
```

## 阶段路线图
- [x] **① 规则引擎**：牌模型、发牌、牌型识别（含逢人配）、压牌比较、炸弹体系 —— 已完成并通过 35 项测试。
- [x] **② 牌局流程**：发牌→进贡/还贡/抗贡→轮转出牌→接风→记名次→升级（含过A）、整场比赛 —— 已完成并通过 23 项测试。
- [x] **③ AI 五档**：完整出牌生成器（全牌型，逢人配感知）+ 入门/中级/高级/大师/宗师 五档策略 —— 已完成。对抗测试梯度显著：宗师/大师 vs 入门 = 40:0，高级 vs 中级 = 36:4（17 项测试全过）。队友与两对手可各自独立设档。
- [x] **④ H5 对局界面**：完整人机对局 —— 选牌/出牌/提示/不要、合法性校验、AI 自动应牌、进贡接风升级过A全流程、结算与设置面板、手机/平板响应式、离线可玩。
- [ ] **⑤ 联机版**：复用引擎，加房间/同步/断线重连（后续）。

## 怎么玩
1. 双击 `index.html` 打开（离线即可）。默认你坐南、对家(北)=高级、左右对手=中级，从打 2 开始。
2. 轮到你：点牌选中（金色抬起）→「出牌」；跟不起点「不要」；不会出点「提示」（可连点切换候选）。
3. **🎴 理牌**：一键把手牌整理成「最少手数」的最优牌型组合（炸弹/同花顺/钢板/连对/顺子/三带二/三/对/散张分组展示），并提示「整理为 N 手可走完」。
4. **🤖 托管**：一键让 AI 替你打，随时可「取消托管」收回操作。
5. 想改难度/起始级牌：点右上「⚙ 设置」，分别给对家/上家/下家设档（入门→宗师），「开始新整场」。
6. 一路打到 A 并在 A 上拿头游即「过 A」获胜。

## AI 五档设计（阶段③）
| 档位 | 行为概述 |
|---|---|
| 入门 | 半随机；只识别单/对；先出小牌；几乎不会用炸弹与配合。 |
| 中级 | 基本策略：出最小可压牌；保留炸弹；简单跟牌与接管判断。 |
| 高级 | 手牌最优拆分（成型组合）；记牌计数；基础进贡与队友配合。 |
| 大师 | + 对手建模、记忆出牌、炸弹时机、接风/报牌意识、队友信号。 |
| 宗师 | + 启发式搜索、残局求解、概率推断对手手牌，接近最优。 |

## 设计原则
- **引擎纯函数、可测试、与 UI/AI 解耦**：`engine.js` 不依赖 DOM，Node 与浏览器通用。
- **离线零依赖**：普通 `<script>` 加载，`file://` 可跑。
- **可复现**：发牌支持随机种子，便于调试与复盘。
